[package]
name = "qtom-cuda"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[features]
default = []
cuda-runtime = []

[dependencies]
qtom-core = { path = "../qtom-core" }
